终于明白这个所谓的回溯是什么意思了。
就是维护一个全局的used数组，表示当前用了哪些元素。
然后随着递归动态修改used数组。当used数组的长度满足一定条件（比如等于输出长度时，就输出）。
对于used的长度，有两种情况：
1.顺序遍历构造，则用level表示在第几层，参考t_17
2.需要回溯撤回，则用used_length动态表示used的长度，参考t_39和t_22
这都是标准的回溯算法操作
不要把局部变量全局化！